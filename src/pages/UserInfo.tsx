import React from 'react';
import UserContainer from '../components/UserInfo/UserContainer';

const UserInfo = () => {
  return (
    <main className="default-dark-main dark:bg-[#001E3C]">
      <UserContainer />
    </main>
  );
};

export default UserInfo;
