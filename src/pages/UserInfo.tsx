import React from 'react';
import UserContainer from '../components/UserInfo/UserContainer';

const UserInfo = () => {
  return (
    <main className="default-dark-main">
      <UserContainer />
    </main>
  );
};

export default UserInfo;
