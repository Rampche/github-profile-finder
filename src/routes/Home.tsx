import React from 'react';
import Container from '../components/Container';

const Home = () => {
  return (
    <main className="bg-sky-800 min-h-screen flex justify-center items-center">
      <Container />
    </main>
  );
};

export default Home;
