export type UserProps = {
  avatar_url: string;
  login: string;
  name: string;
  followers: number;
  following: number;
  public_repos: number;
  bio: string;
  location: string;
};

export type UserInfo = {
  userInfo: string;
};
